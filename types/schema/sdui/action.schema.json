{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "sdui_action",
	"title": "UI_RowAction",
	"description": "A conditional action attached to an SDUI row. Branches are inline action expression strings; the empty string is the canonical \"do nothing\" branch.",
	"type": "object",
	"additionalProperties": false,
	"required": ["condition", "false", "true"],
	"properties": {
		"condition": {
			"type": "string"
		},
		"false": {
			"$ref": "#/$defs/UI_ActionBranch"
		},
		"true": {
			"$ref": "#/$defs/UI_ActionBranch"
		}
	},
	"$defs": {
		"UI_ActionBranch": {
			"description": "Either the empty string (do nothing) or a single inline action expression `{fn(arg, …)}` validated by the shared action parser.",
			"type": "string"
		}
	}
}
