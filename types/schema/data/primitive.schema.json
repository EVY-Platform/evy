{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "primitive",
	"title": "DATA_PRIMITIVE",
	"description": "Generic service-owned data row (namespace is implied by the owning service). Used by downstream services for their Data table.",
	"type": "object",
	"additionalProperties": false,
	"required": ["id", "resource", "data", "created_at", "updated_at"],
	"properties": {
		"id": {
			"type": "string",
			"format": "uuid"
		},
		"resource": {
			"type": "string",
			"maxLength": 50
		},
		"data": {
			"$ref": "../common/json.schema.json#/$defs/JSONValue"
		},
		"created_at": {
			"type": "string",
			"format": "date-time"
		},
		"updated_at": {
			"type": "string",
			"format": "date-time"
		},
		"deleted_at": {
			"description": "Set when the row is deleted. Tombstoned rows are excluded from plain reads but included in incremental ones.",
			"type": "string",
			"format": "date-time"
		}
	}
}
