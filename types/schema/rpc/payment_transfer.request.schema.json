{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "payment-transfer-request",
	"title": "PaymentTransferRequest",
	"description": "JSON-RPC params for transferring captured funds to the seller. V1 records the transfer as an evy.transactions row with type \"transfer\"; a real Stripe Connect transfer will be created later.",
	"type": "object",
	"additionalProperties": false,
	"required": ["payment_intent_id"],
	"properties": {
		"payment_intent_id": {
			"type": "string",
			"minLength": 1,
			"description": "The payment_provider_transaction_id shared by the intent and capture rows."
		}
	}
}
