{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "delete-request",
	"title": "DeleteRequest",
	"description": "JSON-RPC params for delete method. Resource and filter.id are required.",
	"type": "object",
	"additionalProperties": false,
	"required": ["resource", "filter"],
	"properties": {
		"resource": {
			"$ref": "../common/rpc.schema.json#/$defs/ResourceRef"
		},
		"filter": {
			"type": "object",
			"additionalProperties": false,
			"required": ["id"],
			"properties": {
				"id": {
					"type": "string",
					"minLength": 1
				},
				"expected_updated_at": {
					"description": "Optimistic lock. When present, the write only applies if the stored row still carries this updated_at; otherwise it is rejected as a conflict. Omit to accept last-write-wins.",
					"type": "string",
					"format": "date-time"
				}
			}
		}
	}
}
