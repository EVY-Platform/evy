{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "delete-request",
	"title": "DeleteRequest",
	"description": "JSON-RPC params for delete method. Service, resource, and filter.id are required.",
	"type": "object",
	"additionalProperties": false,
	"required": ["service", "resource", "filter"],
	"properties": {
		"service": {
			"type": "string",
			"minLength": 1
		},
		"resource": {
			"type": "string",
			"minLength": 1,
			"maxLength": 50
		},
		"filter": {
			"type": "object",
			"additionalProperties": false,
			"required": ["id"],
			"properties": {
				"id": {
					"type": "string",
					"minLength": 1
				},
				"expected_updated_at": {
					"description": "Optimistic lock. When present, the write only applies if the stored row still carries this updated_at; otherwise it is rejected as a conflict. Omit to accept last-write-wins.",
					"type": "string",
					"format": "date-time"
				}
			}
		}
	}
}
