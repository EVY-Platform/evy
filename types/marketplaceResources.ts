/**
 * Marketplace service + resource UUIDs — single source of truth.
 *
 * Re-exports from the generated constants file.
 * Edit the source at types/schema/resources/marketplace.resources.json and run
 * `bun run types:generate` to regenerate.
 */

export * from "./generated/ts/marketplaceResources";
