{
	"name": "frodo",
	"version": "0.0.1",
	"description": "Frodo admin app",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"lint": "tslint -c tslint.json -p tsconfig.json --fix",
		"build": "tsc",
		"start": "node .",
		"dev": "nodemon --watch src -e ts --exec 'yarn build && yarn start'"
	},
	"license": "GPL-3.0-only",
	"devDependencies": {
		"@types/node": "^18.11.9",
		"nodemon": "^2.0.22",
		"tslint": "^6.1.3",
		"typescript": "^5.1.6"
	},
	"dependencies": {
		"dotenv": "^16.3.1",
		"uuid": "^9.0.0"
	}
}