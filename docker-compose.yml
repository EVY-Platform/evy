services:
  postgres:
    image: "postgres:16"
    ports:
      - ${DB_PORT}:5432
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${DB_EVY_DATABASE}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/postgres-init-marketplace.sql:/docker-entrypoint-initdb.d/02-marketplace.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5

  marketplace:
    build:
      context: .
      dockerfile: ./services/marketplace/Dockerfile
      target: runner
    ports:
      - ${MARKETPLACE_GRPC_PORT}:${MARKETPLACE_GRPC_PORT}
    environment:
      # Listen on all interfaces inside the container (do not use host `.env` here).
      - MARKETPLACE_GRPC_HOST=0.0.0.0
      - MARKETPLACE_GRPC_PORT=${MARKETPLACE_GRPC_PORT}
      - DB_MARKETPLACE_DATABASE=${DB_MARKETPLACE_DATABASE}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - DB_PORT=5432
      - DB_DOMAIN=postgres
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "bun", "run", "health"]
      interval: 500ms
      timeout: 1s
      retries: 10
      start_period: 100ms

  api:
    build:
      context: .
      dockerfile: ./api/Dockerfile
      target: runner
    ports:
      - ${API_PORT}:${API_PORT}
    environment:
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - DB_PORT=5432
      - DB_DOMAIN=postgres
      - DB_EVY_DATABASE=${DB_EVY_DATABASE}
      - API_PORT=${API_PORT}
      # gRPC client target (Compose DNS name), not the marketplace bind address in `.env`.
      - MARKETPLACE_GRPC_HOST=marketplace
      - MARKETPLACE_GRPC_PORT=${MARKETPLACE_GRPC_PORT}
    depends_on:
      postgres:
        condition: service_healthy
      marketplace:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "bun", "run", "health"]
      interval: 500ms
      timeout: 1s
      retries: 10
      start_period: 100ms

  web:
    build:
      context: .
      dockerfile: web/Dockerfile
      target: runner
      args:
        API_URL: ${API_URL}
    ports:
      - ${WEB_PORT}:${WEB_PORT}
    environment:
      - WEB_PORT=${WEB_PORT}
      - API_URL=${API_URL}
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres_data:
