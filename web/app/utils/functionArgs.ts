/**
 * Re-exported from evy-types so the API-side flow validator and the builder
 * split arguments identically. See types/grammar/conformance.json, which pins
 * this behaviour against the Swift implementation.
 */
export { splitFunctionArguments } from "evy-types/functionArgs";
