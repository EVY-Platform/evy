{
	"name": "evy-web",
	"version": "1.0.0",
	"description": "EVY Web App Builder",
	"license": "GPL-3.0-only",
	"type": "module",
	"scripts": {
		"dev": "bun --env-file=../.env run dev/server.ts",
		"build": "bun --env-file=../.env run dev/build.ts",
		"start": "bun --env-file=../.env run dev/server.ts",
		"lint": "bunx biome check .",
		"format": "bunx biome format --write .",
		"setup": "mkdir -p dist/.well-known/appspecific && cp app/index.html dist/ && cp -r app/public/* dist/ && cp app/globals.css dist/ && cp -r app/.well-known/* dist/.well-known/",
		"test:unit": "bun test --define __API_URL__='\"ws://127.0.0.1:1\"' app/",
		"test:integration": "bun --env-file=../.env run playwright test --config=integration/pw.config.js",
		"test:e2e": "bun --env-file=../.env run playwright test --config=e2e/pw.config.js",
		"test:setup": "bunx playwright install --with-deps chromium"
	},
	"dependencies": {
		"@atlaskit/pragmatic-drag-and-drop": "^1.8.1",
		"@atlaskit/pragmatic-drag-and-drop-auto-scroll": "^2.1.5",
		"@atlaskit/pragmatic-drag-and-drop-hitbox": "^1.1.0",
		"lucide-react": "^1.16.0",
		"react": "^19.2.6",
		"react-dom": "^19.2.6",
		"rpc-websockets": "^10.0.1",
		"tiny-invariant": "^1.3.3"
	},
	"devDependencies": {
		"@playwright/test": "1.60.0",
		"@biomejs/biome": "^2.4.15",
		"@types/bun": "^1.3.14",
		"@types/react": "^19.2.14",
		"@types/react-dom": "^19.2.3"
	}
}
