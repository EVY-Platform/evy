name: web

services:
    app:
        build:
            context: ./
            dockerfile: ./Dockerfile
            target: runner
        environment:
            - WEB_PORT=${WEB_PORT:-3000}
        env_file:
            - .env
        ports:
            - ${WEB_PORT:-3000}:${WEB_PORT:-3000}
