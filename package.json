{
	"name": "evy",
	"description": "EVY monorepo - api, web, ios, services and shared types",
	"license": "GPL-3.0-only",
	"scripts": {
		"dev:postgres": "docker compose up postgres",
		"dev:setup": "docker compose up --wait postgres && bun run db:seed",
		"dev:api": "bun run --cwd api dev",
		"dev:web": "bun run --cwd web dev",
		"dev:marketplace": "bun run --cwd services/marketplace dev",
		"dev:watch": "bun scripts/watch.ts",
		"dev": "bun run dev:setup && bun run --parallel --no-exit-on-error dev:postgres dev:api dev:web dev:marketplace dev:watch",
		"format": "bun run --cwd api format && bun run --cwd web format && bun run --cwd services/marketplace format && bunx biome format --write scripts && xcrun swift-format format --in-place --recursive ios",
		"install:all": "bun install && bun install --cwd api && bun install --cwd web && bun install --cwd services/marketplace",
		"types:generate": "bun scripts/generate-types.ts && bun scripts/generate-drizzle.ts && bun scripts/generate-core-resources.ts && bun scripts/generate-marketplace-resources.ts && bunx biome format --write types/generated/ts/",
		"types:generate:exclude-ios": "bun scripts/generate-types.ts --exclude-ios && bun scripts/generate-drizzle.ts && bun scripts/generate-core-resources.ts --exclude-ios && bun scripts/generate-marketplace-resources.ts --exclude-ios && bunx biome format --write types/generated/ts/",
		"db:seed": "bun scripts/seed.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"ajv": "^8.20.0",
		"ajv-formats": "^3.0.1",
		"bun-types": "^1.3.14",
		"concurrently": "^9.0.0",
		"decamelize": "^6.0.1",
		"drizzle-orm": "^0.45.2",
		"json-schema-to-typescript": "^15.0.4"
	},
	"dependencies": {
		"quicktype": "^23.2.6"
	}
}
