{
	"name": "frodo",
	"version": "0.0.1",
	"description": "One platform to rule them all",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"lint": "tslint -c tslint.json -p tsconfig.json --fix",
		"prisma": "prisma generate",
		"migrate": "prisma migrate deploy",
		"build": "tsc",
		"start": "yarn migrate && node .",
		"dev": "nodemon --watch src -e ts,ejs --exec 'yarn build && yarn start'",
		"test": "yarn build && mocha tests/*.spec.js"
	},
	"license": "GPL-3.0-only",
	"devDependencies": {
		"@types/chai": "^4.3.5",
		"@types/mocha": "^10.0.1",
		"@types/node": "^18.11.9",
		"@types/uuid": "^9.0.2",
		"@types/ws": "^8.5.5",
		"chai": "^4.3.7",
		"mocha": "^10.2.0",
		"nodemon": "^2.0.22",
		"prisma": "^4.16.2",
		"tslint": "^6.1.3",
		"typescript": "^5.1.6"
	},
	"dependencies": {
		"@prisma/client": "^4.16.2",
		"dotenv": "^16.3.1",
		"rpc-websockets": "^7.5.1",
		"uuid": "^9.0.0"
	}
}