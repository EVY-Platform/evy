name: api

services:
  app:
    build:
      context: ./
      dockerfile: ./Dockerfile
      target: runner
    environment:
      - API_PORT=${API_PORT:-8000}
      - DB_URL=${DB_URL}
    env_file:
      - .env
    ports:
      - ${API_PORT:-8000}:${API_PORT:-8000}
